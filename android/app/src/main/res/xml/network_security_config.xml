<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration: Disable cleartext traffic in production -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Debug overrides: Allow localhost and user certificates for development -->
    <debug-overrides>
        <base-config cleartextTrafficPermitted="true">
            <trust-anchors>
                <!-- Trust system certificates -->
                <certificates src="system" />
                <!-- Trust user-installed certificates (for development/testing) -->
                <certificates src="user" />
            </trust-anchors>
        </base-config>
    </debug-overrides>
    
    <!-- Domain-specific configurations -->
    <domain-config cleartextTrafficPermitted="false">
        <!-- Google APIs - HTTPS only -->
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">accounts.google.com</domain>
        <domain includeSubdomains="true">oauth2.googleapis.com</domain>
        <domain includeSubdomains="true">gmail.googleapis.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
    
    <!-- Allow localhost only in debug builds (for development) -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Lint requires includeSubdomains attribute on each domain -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain> <!-- Android emulator host -->
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>

