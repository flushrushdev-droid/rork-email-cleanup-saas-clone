import { StyleSheet } from 'react-native';
import type { ThemeColors } from '@/constants/colors';

export function createEmailDetailStyles(colors: ThemeColors) {
  return StyleSheet.create({
    container: {
      flex: 1,
    },
  });
}


