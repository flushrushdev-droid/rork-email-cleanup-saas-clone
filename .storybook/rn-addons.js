// On-device addons are currently disabled due to compatibility issues
// They require storybook/manager-api which is web-only
// Storybook will work without these addons - you can still view and interact with stories

// Uncomment these if you install the core storybook package:
// import '@storybook/addon-ondevice-actions/register';
// import '@storybook/addon-ondevice-notes/register';

// Note: @storybook/addon-ondevice-controls requires manager-api and won't work

