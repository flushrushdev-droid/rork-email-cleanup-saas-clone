# Environment Variables Configuration
# Copy this file to .env and fill in your values
# For Rork development, these can be set in the Rork dashboard
# For Vercel/Render/Supabase, set these in your deployment platform's environment variables

# ============================================
# App Configuration
# ============================================

# Environment: development, staging, or production
EXPO_PUBLIC_APP_ENV=development

# App scheme for deep linking (e.g., 'rork-app', 'athenxmail')
EXPO_PUBLIC_APP_SCHEME=rork-app

# Base URL for OAuth redirects (used for web OAuth callbacks)
# For Rork: https://rork.com
# For Vercel: https://your-app.vercel.app
# For Render: https://your-app.onrender.com
# Leave empty to auto-detect (web: uses current origin, native: uses network IP or Rork URL)
EXPO_PUBLIC_REDIRECT_BASE_URL=https://rork.com

# Local Development IP (for mobile testing on local network)
# Set this to your computer's local network IP address (e.g., 192.168.1.100)
# Find it by running: ipconfig (Windows) or ifconfig (Mac/Linux)
# Only needed for local mobile testing - leave empty for production
# EXPO_PUBLIC_LOCAL_DEV_IP=192.168.1.100

# ============================================
# API Configuration
# ============================================

# API Base URL (optional - will auto-detect for web, defaults to localhost:3000 for native)
# For Rork: Leave empty or set to your Rork API URL
# For Vercel: https://your-api.vercel.app
# For Render: https://your-api.onrender.com
# For Supabase: https://your-project.supabase.co
EXPO_PUBLIC_API_BASE_URL=

# Legacy Rork API Base URL (for backward compatibility)
# EXPO_PUBLIC_RORK_API_BASE_URL=

# ============================================
# Google OAuth Configuration
# ============================================

# Google OAuth Client ID
# Get this from: https://console.cloud.google.com/apis/credentials
# Create separate OAuth clients for development, staging, and production
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (Web only - required for web OAuth even with PKCE)
# WARNING: For production, this should be moved to a backend endpoint for better security
# For native apps (Android/iOS), this is NOT needed - PKCE is sufficient
# Get this from: https://console.cloud.google.com/apis/credentials
EXPO_PUBLIC_GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# ============================================
# Feature Flags
# ============================================

# Enable demo mode (true/false)
EXPO_PUBLIC_ENABLE_DEMO_MODE=true

# Enable debug logging (true/false)
EXPO_PUBLIC_ENABLE_DEBUG_LOGGING=false

# ============================================
# Error Logging (Sentry)
# ============================================

# Sentry DSN for error tracking
# Get this from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
# Leave empty to disable error logging (errors will still be logged to console)
# EXPO_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id

# Enable Sentry in development mode (true/false)
# By default, Sentry is disabled in development to avoid noise
# Set to 'true' to enable Sentry error tracking during development
# EXPO_PUBLIC_SENTRY_ENABLE_IN_DEV=false

# ============================================
# Backend Environment Variables
# ============================================
# These are for backend/server-side use only
# They should NOT be prefixed with EXPO_PUBLIC_

# Database URL (for Supabase/PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# Supabase Configuration
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google OAuth Client Secret (Backend/Server-side use)
# Note: For web OAuth, Google requires client_secret even with PKCE, so it's also in EXPO_PUBLIC_ above
# For backend token exchange or server-side flows, use this variable (no EXPO_PUBLIC_ prefix)
# GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# JWT Secret (for backend authentication)
# JWT_SECRET=your-jwt-secret-key

# CORS Origins (comma-separated list)
# CORS_ORIGINS=https://your-app.vercel.app,https://your-app.onrender.com

# ============================================
# Notes
# ============================================
# - Variables prefixed with EXPO_PUBLIC_ are exposed to the client
# - Never commit .env files to version control
# - Use .env.example as a template for your .env file
# - For production, set these in your deployment platform's environment variables

